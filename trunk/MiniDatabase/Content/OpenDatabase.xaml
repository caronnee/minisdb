<Window x:Class="MiniDatabase.Content.OpenDatabase"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <HeaderedItemsControl Header="aldhakjdfh">
            <ListBox x:Name="Databases"  Margin="10" ItemsSource="{Binding Filenames}" SelectedValue="{Binding Filename, Mode=OneWay}">
            </ListBox>
        </HeaderedItemsControl>
        
        <StackPanel Grid.Row="1"  Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Width="100" IsCancel="True">Cancel</Button>
            <Button Width="100" IsDefault="True" Click="LoadDatabase">Ok</Button>
        </StackPanel>
    </Grid>
</Window>
